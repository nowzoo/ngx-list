import { NgxListFilterFactory } from './ngx-list-filter-factory';

describe('NgxListFilterFactory', () => {
  it('should create an instance', () => {
    expect(new NgxListFilterFactory()).toBeTruthy();
  });
});
